extends base
include mixins/video

block content
	div.video-grid
		each video in videos
			+video(video)
			+video(video)
			+video(video)
			+video(video)
			+video(video)
			+video(video)
			+video(video)
			+video(video)
		else
			span.empty_message No Video Found.